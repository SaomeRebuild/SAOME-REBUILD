# Local dev: use Vite proxy to production backend (same-origin /api).
# The Vite proxy in vite.config.ts forwards /api/* to the deployed backend,
# so cookies are stored as same-origin and SameSite=Lax survives.
VITE_API_BASE_URL=
VITE_APP_BASE_URL=http://localhost:5173
