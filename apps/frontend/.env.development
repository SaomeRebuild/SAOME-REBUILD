# Dev: use Vite proxy (same-origin) - backend at http://127.0.0.1:8787 via Vite proxy
# This avoids CORS issues and is the recommended dev setup
VITE_API_BASE_URL=
VITE_APP_BASE_URL=http://localhost:5174
