---
description: SAOME 開發方法論 — 全部依賴 GitHub spec-kit 與 Superpowers skills。SAOME 不再自製流程。
alwaysApply: true
---

# 開發方法論：spec-kit + Superpowers

> **2026-07-25 重整**：原本 SAOME 自製的「SDD → BDD → TDD → Verification」四階段已**全部刪除**，改為完全依賴 [GitHub spec-kit](https://github.com/github/spec-kit) 與 [Superpowers](https://github.com/superpowers) skill。
> 本檔是 source-of-truth 入口，所有細節由對應 skill 與 `.specify/memory/constitution.md` 提供。

## 入口零：自然語言意圖識別（MANDATORY）

任何使用者訊息**先**走 `saome-intent-router` skill 識別動詞語意（add / remove / exclude / implement / amend / refactor / migrate / deprecate）→ AskQuestion 確認（最多 1-2 題）→ 委派給對應的 spec-kit 流程或 workflow。

**例外**：純問答（「看 / 解釋 / 為什麼 / 哪裡」）不需 intent-router，**直接**走 `saome-skill-router` 模式選擇 SOP（Ask 模式）。

詳見 `.cursor/skills/saome-intent-router/SKILL.md`。

## 入口一：流程（單一）

```
1. /speckit.constitution ← 建立 / 編輯專案原則
2. /speckit.specify      ← 從需求產生 spec.md
3. /speckit.clarify      ← 對 spec 提問補完（可選）
4. /speckit.plan         ← 從 spec 產生 plan.md
5. /speckit.tasks        ← 從 plan 拆解 tasks.md
6. /speckit.checklist    ← 產生驗收 checklist
7. /speckit.implement    ← 依 tasks 執行（搭配 Superpowers TDD）
8. /speckit.analyze      ← 對齊 spec/plan/tasks
9. /speckit.converge     ← 對 codebase 找剩餘工作
```

詳細行為請 `Read` `.cursor/skills/speckit-*/SKILL.md`（10 個）。

## Superpowers 流程 skill（Cursor 強制載入）

> 透過 `using-superpowers` skill 載入，AGENTS.md Step 0 已強制。

- `brainstorming` — 規格前收需求
- `writing-plans` — 計畫前架構
- `test-driven-development` — Red → Green → Refactor 細則
- `refactoring` — Green 完成後
- `verification-before-completion` — 完成宣告前必跑
- `systematic-debugging` — bug 排查

## 細則位置（不再分散）

| 主題 | 檔案 |
|---|---|
| 專案原則 | `.specify/memory/constitution.md` |
| 規格模板 | `.specify/templates/spec-template.md` |
| 計畫模板 | `.specify/templates/plan-template.md` |
| 任務模板 | `.specify/templates/tasks-template.md` |
| 檢查清單模板 | `.specify/templates/checklist-template.md` |
| PowerShell 腳本 | `.specify/scripts/powershell/*.ps1` |
| 多租戶鐵律 | `.cursor/rules/002-multi-tenant.mdc` |
| 安全鐵律 | `.cursor/rules/004-security.mdc` |
| i18n 規範 | `.cursor/rules/003-i18n.mdc` |
| 模組化 | `.cursor/rules/000-modular-design.mdc` |
| 動態化 | `.cursor/rules/000-dynamic-config.mdc` |
| 店家設定 | `.cursor/rules/000-business-constants.mdc` |

## 禁止

- ❌ 自行定義 SDD / BDD / TDD 流程（必須用 spec-kit + Superpowers）
- ❌ 手寫 spec.md / plan.md / tasks.md（必須由對應 slash command 產生）
- ❌ 跳過 `/speckit.constitution` 直接 `/speckit.specify`
- ❌ 跳過 TDD Red → Green → Refactor
- ❌ 跳過 `verification-before-completion`
- ❌ 跳過 `saome-intent-router` 直接 brainstorm（動詞未識別就跑 brainstorming）
- ❌ 「之後補 spec / test / verification」一律視為 P1 違規