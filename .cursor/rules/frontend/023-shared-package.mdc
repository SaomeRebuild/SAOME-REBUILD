---
description: shared Package 邊界 — frontend/ 只能放 Web 特定內容，可共用的程式碼必須 packages/shared/。
alwaysApply: true
---

# shared Package 邊界（MANDATORY）

> frontend/ 與 packages/shared/ 的責任劃分，是 monorepo 的關鍵。

## packages/shared/ 放什麼 ✅

| 類型 | 範例 |
|------|------|
| TypeScript 介面 | `Member`, `Order`, `Pass` interface |
| zod schemas | `memberSchema`, `passSchema` |
| 業務邏輯純函式 | `validateEmail()`, `calcDiscount()` |
| 常數 | `MEMBER_ROLE`, `PASS_TIER` |
| API contracts | request/response types |
| i18n 翻譯檔 | zh-TW.ts, en.ts |
| BDD feature | `.feature` 與 step definitions |

## frontend/（apps/frontend/）放什麼 ✅

| 類型 | 範例 |
|------|------|
| React 元件 | `<Button>`, `<MemberCard>` |
| React Hook | `useAuth`, `useTableQuery` |
| Web 特定 hook | `useBreakpoint`, `useIntersection` |
| Vite config | vite.config.ts |
| Tailwind config | tailwind.config.ts |
| Storybook | stories |

## 禁止 ❌

- ❌ 在 apps/frontend/ 內寫可共用的業務邏輯
- ❌ 在 apps/frontend/ 直接呼叫 `fetch` / `localStorage`（要包成 service / hook）
- ❌ 把 TypeScript 介面定義在 app 內（要搬 packages/shared/）
- ❌ 把 i18n key 寫在 app 內（要搬 packages/shared/）

## i18n Namespace 規範（MANDATORY）

> 觸發：新增 i18n namespace、建立翻譯檔。

### 命名規則

| 規則 | 說明 |
|---|---|
| **namespace key** | 必為 camelCase（如 `passNotification`、`landing`） |
| **檔名格式** | `{namespace}.{locale}.ts`（如 `passNotification.zh-TW.ts`） |
| **禁止 hyphen** | 禁止在 namespace key 使用 hyphen（如 `pass-notification`） |
| **禁止 `translation`** | 廢除 `translation` namespace；所有 key 必須分散至 feature namespace |

### 元件化原則（MANDATORY）

> 每個**業務元件**（L2）需要翻譯時，應該有自己的 **component-bound namespace**，而不是放在 feature namespace。

#### 為什麼要元件化

| 情境 | 問題 |
|------|------|
| 把 `PassCardPreview` 翻譯放在 `cardBuilder` namespace | `PassCardPreview` 跨 feature 重用時，必須攜帶整個 `cardBuilder` namespace |
| 把 `PassCardPreview` 翻譯放在 `cardBuilder` namespace | 翻譯 key 結構跟 namespace 名稱不同（`passCard.defaultName` vs `cardBuilder`） |
| 多人開發時改 `cardBuilder` namespace | 影響 `PassCardPreview` 的翻譯，耦合過高 |

#### 何時用 component namespace（首選）

- L2 元件跨 feature 重用
- 例：`passCard`（`PassCardPreview`、`TemplateCardPreview` 都用）
- 例：`memberBadge`（跨 Dashboard / Member 頁面使用）

#### 何時用 feature namespace 的 sub-key

- 多個 L2 元件共用同一套翻譯，且都在同一 feature 內
- 例：`dashboard.cardTypeSelector.*`（`CardTypeSelector` + `CardBuilderEditor` 共同使用）

#### 何時用共享的 feature namespace

- 頁面 layout 翻譯（如 `dashboard.header.*`）
- 全域 UI 元素（如 `nav.*`）
- 多個 feature 共同使用（如 `theme.*`）

#### 實作範例

```typescript
// ✅ 正確：元件與 namespace 一對一綁定
// namespace: passCard
// 元件：PassCardPreview, TemplateCardPreview（共用語，翻譯在 passCard）
const { t } = useTranslation('passCard');
t('defaultName'); // 在 passCard namespace 內找 key
```

```typescript
// ❌ 錯誤：把元件翻譯放在 feature namespace
// namespace: cardBuilder
// 元件：PassCardPreview（不該用 cardBuilder namespace）
const { t } = useTranslation('cardBuilder');
t('passCard.defaultName'); // 變成 cardBuilder.passCard.defaultName
```

### 新增 Namespace 的 Checklist（必跑）

> 新增任何 i18n namespace 前，**必須**逐項確認。

#### 1. 元件化判斷

| 問題 | 回答 |
|------|------|
| 這個翻譯屬於哪個 L2 元件？ | 找對應的 component namespace |
| 元件是否跨 feature 重用？ | 是 → component namespace；否 → feature namespace |
| 多個元件是否共用同一套翻譯？ | 是 → feature namespace 的 sub-key |

#### 2. key 結構確認

| 檢查 | 標準 |
|------|------|
| 翻譯檔是 **flat key** | ✅ `export default { defaultName: '未命名卡片' }` |
| 不是 nested object | ❌ `export default { passCard: { defaultName: '未命名卡片' } }` |
| 對照既有範例 | `auth`、`dashboard` namespace 的 key 結構 |

#### 3. 使用方式確認

| 正確 | 錯誤 |
|------|------|
| `useTranslation('passCard')` + `t('defaultName')` | `t('passCard.defaultName')`（多了 namespace 前綴） |

#### 4. 同步 test i18n

- `apps/frontend/src/test/i18n.ts` **必須**同步 import + resources
- 否則 unit test 環境無法正確解析翻譯

#### 5. 執行 smoke test

```bash
npm run verify:i18n  # 驗證 namespace 的 key 都可正確解析
```

### 原因

`translation` namespace 會隨 feature 增加而無限膨脹。單一檔案的缺點：
- 新增 key 時容易放錯位置
- 檔名 vs resources key mismatch（`passNotification.zh-TW.ts` vs `passNotification`）難以察覺
- 未來 React Native 化時難以拆分

### 現有 Namespace 清單

| namespace | 用途 | 檔案 |
|---|---|---|
| `auth` | 登入、註冊、表單驗證 | `auth.{locale}.ts` |
| `dashboard` | Dashboard layout | `dashboard.{locale}.ts` |
| `landing` | Landing Page、Hero、Features、定價說明 | `landing.{locale}.ts` |
| `legal` | 服務條款、隱私權、DPA | `legal.{locale}.ts` |
| `passNotification` | Pass 通知（trial、renewalReminder） | `passNotification.{locale}.ts` |
| `pricing` | 定價頁 | `pricing.{locale}.ts` |
| `theme` | 主題切換 | `theme.{locale}.ts` |
| `nav` | 導航 | `nav.{locale}.ts` |
| `member` | 會員相關 | `member.{locale}.ts` |

### 翻譯書寫紀律（MANDATORY）

| 語言 | 規定 |
|---|---|
| zh-TW | 嚴格使用全中文，禁止摻雜英文單字（如 `Pass`、`QR Code` 應改為 `卡片`、`QR 碼`） |
| en | 嚴格使用全英文，禁止摻雜中文或其他語言 |

理由：中英夾雜對非英語人士造成閱讀障礙，且破壞翻譯風格一致性。

例外：若某英文術語有約定俗成的中文譯名（如 `API`、`URL`），可使用該譯名，但禁止直接寫原形。

### 觸發關鍵字

「新增 i18n」「翻譯 key」「namespace」「i18n 檔案」**必須**引用本節。

## 結構範例

```
packages/shared/
├── types/         ← TypeScript 介面
├── schemas/       ← zod
├── constants/     ← 常數
├── logic/         ← 純函式
├── api/           ← API contract
├── i18n/          ← 翻譯檔
└── bdd/           ← Cucumber feature + step

apps/frontend/
├── src/
│   ├── components/
│   ├── hooks/             ← Web 特定 hook
│   ├── pages/
│   ├── config/            ← frontend 環境常數
│   └── services/          ← BFF 呼叫，內部用 packages/shared 的型別
```

## 觸發關鍵字

「共用」「共用函式」「shared」「type」「interface」「i18n 翻譯」**必須**引用。
