---
description: DB ↔ zod schema ↔ backend request schema 跨 package 契約鐵律 — 任何改 backend schema 或 migration 的 PR 必須引用
alwaysApply: true
---

# Schema Contract Drift（MANDATORY）

> 觸發：改 `apps/backend/src/modules/auth/schemas/request.ts`（或任何
> `request.ts` / `dto.ts`）、新增 DB migration、改 `packages/shared/schemas/`。

## 背景

2026-07-31 register 流程 Round 1+2 事故：

- Round 1：`tenants` table 缺 `email`、`mobile`、`website` 三欄，
  前端 payload 帶了，後端 `insertTenant` 沒欄位可寫，靜悄悄丟掉。
- Round 2：前端 `z.string().min(2)` 過、後端 stub `z.string().min(1)`
  過；後端 `request.ts` 是 hand-written `TODO: cp from shared/`，從沒被
  sync 過。

完整記錄見
`runs/improvements/feedback/20260731-register-autofill-schema-drift.md`。

## 鐵律

### 1. single source of truth — shared zod schema 是契約

`packages/shared/schemas/` **是** 契約。後端的
`src/modules/<module>/schemas/request.ts`（以及其他 `request.ts` /
`dto.ts`）**必須** 從 shared 自動生成或拷貝。

> 手寫一份分歧的副本 = **P0 violation**。

| Pattern | 為什麼 P0 |
|---------|----------|
| 後端 `request.ts` 的 `z.object({...})` 跟 shared `registrationPayloadSchema` 的 field set 不一致 | 前後端 zod 各自漂移，runtime 一邊過一邊 fail |
| `request.ts` 開頭有 `// TODO: cp from shared/` 之類註解 | 表示從來沒被同步過，是技術債定時炸彈 |
| `request.ts` 直接 import z 自己組合，沒共用 shared 的 base shape | 任何 shared 改動這邊不會跟上 |

**修法**（依優先順序）：

1. **首選**：建 `apps/backend/src/shared/schemas/from-shared/` 拷貝
   pipeline，`request.ts` 改成 `export * from '@/shared/schemas/from-shared/auth';`
2. **次選**：monorepo 用 path alias 直接 `export * from '@saome/shared/schemas/auth';`
   （需要 vite alias 跟 vitest alias 都加上，見 rule 016）
3. **過渡期**：後端 stub 跟 shared 改成完全 mirror，每次改 shared
   **必同步**改後端 stub。

### 2. DB ↔ schema binding — 每個 column 必出現三次

任何 `public.<table>` 的欄位，**必須** 同時出現在三個地方，缺一即 P0：

| 出現地點 | 範例 |
|---------|------|
| DB migration | `migrations/00X_init_xxx.sql` 的 `CREATE TABLE` |
| `tenants.ts::insertX` 參數列 | `apps/backend/src/modules/<module>/db/<table>.ts` |
| shared zod schema（optional field） | `packages/shared/schemas/<module>.ts` |

例：2026-07-31 的 `email` 欄位，DB migration 補上後，後端 `tenants.ts::insertTenant`
的 params 跟 shared `tenantInfoSchema` 也要**同個 commit**補上。

### 3. conformance test — 必加 Vitest conformance test

每個 `request.ts` 必加一個 Vitest conformance test，斷言 local schema
跟 shared schema 的 field set 一致：

```ts
// apps/backend/src/modules/auth/tests/schema-conformance.test.ts
import { describe, it, expect } from 'vitest';
import { registrationPayloadSchema as localRegistration } from '../schemas/request';
import { registrationPayloadSchema as sharedRegistration } from '@saome/shared/schemas/auth';

describe('schema conformance (shared vs backend)', () => {
  it('registrationPayloadSchema fields match', () => {
    expect(Object.keys(localRegistration.shape).sort())
      .toEqual(Object.keys(sharedRegistration.shape).sort());
  });
});
```

**建議**（plan optional）：用 `scripts/check-shared-schema-sync.cjs`
當作 build-time check，在 `npm run build` 之前跑，自動 fail。

### 4. migration 紀律 — 新增 column 必 quad-binding

新增 `public.<table>` 欄位的 PR，**必須** 在同一個 commit 內帶齊：

```
[migration .sql]                 # 改 DB schema
[migration .sql - re-run via Supabase MCP apply_migration]
[packages/shared/schemas/...]    # shared zod 加 optional field
[apps/backend/.../db/<table>.ts] # insertX 參數列加欄位
[apps/backend/.../schemas/request.ts]  # backend request schema 加 field（第四層）
[apps/backend/.../services/<service>.ts] # backend service 參數型別
[apps/backend/.../tests/schema-conformance.test.ts]  # 加 conformance 驗證
[feedback]                       # 必要時 runs/improvements/feedback/
```

任一缺一 → 後續 drift 又會出現。

### 4.1 Backend Request Schema（第四層）— 自 2026-08-22 CardBuilder 補上

8/21-22 CardBuilder 實作觀察：`cardType` optional drift 與 `isPaid` extension drift
都在「shared schema ✅ → backend request.ts ❌」這層漏掉。

**四層必須同步**（以 `templateSettingsSchema` 為例）：

| 層 | 位置 | 檢查重點 |
|---|------|---------|
| 1 | `packages/shared/schemas/card.ts` | `templateSettingsSchema` 有這個 field |
| 2 | `apps/backend/src/modules/cards/schemas/request.ts` | `templateSettingsSchema` 有這個 field |
| 3 | `apps/backend/src/modules/cards/db/templates.ts` | `TemplateSettings` interface 有這個 field |
| 4 | `apps/backend/src/modules/cards/services/cardService.ts` | service 參數型別含這個 field |

詳見 `runs/improvements/feedback/20260822-card-builder-draft-abandon-full-trace.md`
§ `isPaid` Extension Schema Drift。

### 6. DB CHECK constraint — zod enum 必同步

DB migration 裡的 `CHECK (column IN ('a', 'b', 'c'))` 是一個**隱性 schema**。
zod enum 必須跟它對齊，否則 application 允許的值 DB 會 reject，反之亦然。

| 範例 | 根因 |
|---|---|
| `passTierSchema = ['basic', 'premium', 'enterprise']` 但 DB CHECK 是 `('green', 'gold', 'platinum')` | typecheck 一直過，但 runtime insert/update fail |
| `passTierSchema` 從未被 runtime 引用（backend 直接拿 DB row 組 object） | 沒有 coverage 保護，drift 不會被發現 |

**鐵律**：每個 enum-type column 的 DB CHECK constraint 必須同步到 zod enum，且**必加 Vitest conformance test**斷言兩邊值集合相同。

```ts
// packages/shared/schemas/pass.test.ts
import { describe, it, expect } from 'vitest';
import { passTierSchema } from '../pass';
import { passSchema as dbPassPlanSchema } from '@saome/shared/schemas/auth';

it('passTierSchema enum values match DB CHECK constraint', () => {
  // DB: passes.plan CHECK (plan IN ('green', 'gold', 'platinum'))
  expect(passTierSchema.options).toEqual(['green', 'gold', 'platinum']);
  // passTierSchema must equal planSchema (same domain)
  expect(passTierSchema.options).toEqual(dbPassPlanSchema.options);
});
```

**修法**：`packages/shared/schemas/pass.ts` 的 `passTierSchema` 註解已加 `// mirrors passes.plan CHECK constraint in DB`，未來任何修改 CHECK constraint 的 migration 必須同步更新此 enum。

### 7. backward compatibility

如果新增欄位要 default（例如 `invoice_address` 從 NOT NULL 變成
NULL-able），**必須** 在 migration 裡明確寫 default value 或 `USING`
clause，**不準** 留給 application 處理。

例：

```sql
-- ✅ 正確
ALTER TABLE public.tenants ADD COLUMN email text NULL;
ALTER TABLE public.tenants ALTER COLUMN email SET NOT NULL;

-- ❌ 錯誤
ALTER TABLE public.tenants ADD COLUMN email text;  -- NOT NULL 預設，但沒 default value → 既有 rows 會 fail
```

## 觸發關鍵字

出現「schema drift」、「後端 zod 跟前端對不上」、「DB column missing」、
「backend schema stub」、「shared/schemas」、「from-shared」、
「新增欄位」、「migration」時**必須**引用此 rule。

### 強化觸發條件（自 2026-08-22）

以下情境**必定觸發**四層同步 check（不得跳過）：

| 情境 | 為什麼 |
|------|--------|
| shared schema 新增 `z.boolean().optional()` / `.optional()` | optional field 特別容易 drift（從不缺到缺） |
| shared schema 的 `z.object` 多了任何 key | 每個 key 都要出現在後端 |
| 新增 DB migration 加 column | migration 是 drift 起點 |
| 任何 `request.ts` 有 `// TODO` 或 `// FIXME` 註解 | 表示落後於 shared，隨時會 drift |
| feature 有 2 個以上 schema drift feedback | 系統性問題，觸發 3 層檢查清單 |

**觸發後必跑**：

```
□ shared schema 有這個 field
□ backend schemas/request.ts 有這個 field
□ backend db/<table>.ts 的 interface 有這個 field
□ backend services/<service>.ts 的參數型別含這個 field
□ conformance test 通過
```

## 參照

- `.cursor/skills/saome-form-integrity/SKILL.md` — Step E schema drift check
- `.cursor/rules/018-form-autofill-and-multi-step-state.mdc` — 同源 bug 的 sibling rule
- `runs/improvements/feedback/20260731-register-autofill-schema-drift.md`
  — Register 表單 autofill + schema drift 三連環事故記錄
- `runs/improvements/feedback/20260822-card-builder-draft-abandon-full-trace.md`
  — CardBuilder `cardType` + `isPaid` drift 完整事件鏈（第四層漏掉的案例）
- `DEV/08-2026/0815-pass-tier-schema-drift.md`
  — PassTier `basic/premium/enterprise` vs DB `green/gold/platinum` drift（`passTierSchema` 從未被 runtime 引用所以 typecheck 一直過）
- `apps/backend/src/modules/auth/schemas/request.ts` — TODO 來源檔
- `apps/backend/migrations/004_init_passes.sql` — Pass plan CHECK constraint 定義
- `packages/shared/schemas/pass.ts` — `passTierSchema`（已加 comment 說明等於 `planSchema`）
- `packages/shared/schemas/auth.ts` — `planSchema`（`passTierSchema` 的 source of truth）