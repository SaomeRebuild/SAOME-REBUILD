---
description: BDD 行為驅動開發流程 — Cucumber 整合，必須從 spec 抽出 .feature 檔案
alwaysApply: true
---

# BDD 工作流程（MANDATORY）

> 任何業務邏輯實作前，必須有對應的 .feature 檔案驗證行為。

## 觸發時機

從 `.specify/memory/specs/` 中的 `spec.md` 抽出 `.feature` 檔案時觸發。

## 檔案位置

| 類型 | 位置 |
|------|------|
| Feature files | `.specify/memory/specs/features/*.feature` |
| Step definitions | `packages/shared/bdd/steps/*.ts` |
| Support files | `packages/shared/bdd/support/*.ts` |
| Cucumber config | `packages/shared/bdd/config.ts` |

## 執行命令

```bash
npm run test:bdd          # 執行所有 BDD 測試
npm run test:bdd:watch   # 監聽模式
```

## 與 TDD 銜接

| 層次 | 驗證目標 | 工具 |
|------|----------|------|
| BDD | 高層次行為（使用者場景） | Cucumber |
| TDD | 低層次實作（單元測試） | Vitest + RTL |

**順序**: BDD 綠燈 → 才能進入 TDD 微調

## Feature File 命名

```
<功能>-<場景>.feature
範例：
- member-registration.feature
- product-page-navigation.feature
```

## Scenario 撰寫規則

### Given-When-Then 結構

```gherkin
Feature: 會員註冊

  Scenario: 正常註冊流程
    Given 我在會員註冊頁面
    And 我填入有效資料
    When 我點擊「註冊」按鈕
    Then 系統顯示「註冊成功」
    And 發送驗證郵件
```

## 禁止

- ❌ 沒 spec 就寫 .feature
- ❌ 跳過 step definitions 直接寫實作
- ❌ Scenario 超過 10 步
- ❌ 同一個 step 重複定義

## 觸發關鍵字

「加業務邏輯」「新增功能」「做頁面」**必須**引用此 rule。

## 參照

- `.specify/memory/constitution.md` — 宪法原則 II. BDD-Validated
- `001-methodology.mdc` — 三層流程整合
- `012-bdd-workflow.mdc` — 詳細 BDD 流程
