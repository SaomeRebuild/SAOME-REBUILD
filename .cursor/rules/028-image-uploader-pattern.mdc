# Image Uploader Pattern（MANDATORY）

> 觸發：建立任何圖片上傳相關元件（LogoUploader、BackgroundUploader、IconUploader 等）。

## 背景

2026-08-23 LogoUploader 實作觀察：

- R2 presigned URL 流程踩了 CORS 三階段坑（origin / R2 endpoint / headers wildcard）
- settings JSON 直接覆蓋踩了 merge 坑
- focal point 拖曳方向踩了負號坑
- Zustand store 時機問題踩了 `lastUploadedLogoKey` 坑
- i18n 放錯 namespace（放進 `cardBuilder` 而非獨立 `logoUpload`）

詳見 `runs/improvements/feedback/20260823-logo-uploader-full-trace.md`。

## 鐵律

### 1. Presigned URL pattern（強制）

所有圖片上傳走這條 chain，Worker 只做簽署：

```
前端 → POST /api/cards/:id/upload-url/:type
     ← { presignedUrl, key }
前端 → PUT presignedUrl（R2 direct）
     ← 200 OK
前端 → PATCH /api/cards/:id（回填 key 到 settings）
```

**禁止**：不走 presigned URL，直接 POST 到 Worker 再 streaming 到 R2（Worker 50ms CPU time budget 撐不住 streaming）。

### 2. Settings merge（強制）

PATCH template 時，settings 必須 fetch → merge → write，**禁止**直接覆蓋：

```typescript
// ✅ 正確
const updateData = {
  ...existingData,
  settings: { ...existingData.settings, ...input.settings },
};

// ❌ 錯誤：直接覆蓋會洗掉其他欄位
const updateData = { ...existingData, settings: input.settings };
```

詳見 Rule 027（postgres dynamic query）。

### 3. Focal point coordinate（強制）

拖曳時坐標計算必須取負號：

```typescript
// ✅ 正確
const dx = e.clientX - dragStart.current.x;
const newFocalX = dragStart.current.focalX - dx / displayWidth;

// ❌ 錯誤：沒取負號 → 拖曳方向與視覺相反
const newFocalX = dragStart.current.focalX + dx / displayWidth;
```

### 4. Preview reliability（強制）

上傳成功後，用 `lastUploadedXxxKey` state 追蹤 key，**優先於** Zustand store：

```typescript
const [lastUploadedLogoKey, setLastUploadedLogoKey] = useState<string | null>(null);

// 上傳成功後
setLastUploadedLogoKey(key);

// render 時：state > store
const displayKey = lastUploadedLogoKey ?? issuerLogoKey;
```

理由：Zustand store 更新時機落後於 API response，可能造成上傳完卻不顯示的問題。

### 5. R2 CORS 四件套（強制）

每次新增 R2 bucket 或調整 CORS 時，必須湊齊四件：

| 項目 | 設定值 | 說明 |
|---|---|---|
| `allowed_origins` | 包含 R2 endpoint | `https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com` |
| `allowed_origins` | 包含所有 frontend origins | `https://saome-frontend.pages.dev` + `https://saome-frontend-*.pages.dev` |
| `allowed_headers` | `["*"]` | aws4fetch 簽署請求帶 `x-amz-*` headers，wildcard 必要 |
| `allowed_methods` | `["PUT", "HEAD"]` | R2 presigned URL 用 PUT 上傳 |

設定檔：`apps/backend/r2-cors.json`。

### 6. Wrangler secrets 順序（強制）

```bash
# ✅ 正確順序
wrangler deploy
wrangler secret put R2_ACCOUNT_ID
wrangler secret put R2_ACCESS_KEY_ID
wrangler secret put R2_SECRET_ACCESS_KEY

# ❌ 錯誤：先設定 secret 再 deploy → 412 Precondition Failed
wrangler secret put R2_ACCOUNT_ID
wrangler deploy  # 失敗
```

### 7. Dev vars 同步（強制）

`.dev.vars` 必須包含與 production secrets 一樣的 key，否則本地 `wrangler dev` 噴 `ReferenceError: R2_ACCOUNT_ID is not defined`：

```bash
# apps/backend/.dev.vars 必填
R2_ACCOUNT_ID=xxx
R2_ACCESS_KEY_ID=xxx
R2_SECRET_ACCESS_KEY=xxx
R2_PUBLIC_URL=https://saome-backend.josh1989213.workers.dev
```

### 8. 裁切 logic 可抽離 Canvas（RN 化必備）

裁切運算（normalize focal point、crop rect）必須寫在 `packages/shared/logic/` 為**純函式**，**不能**依賴 `HTMLCanvasElement`：

```
packages/shared/logic/image-crop/
├── normalizeFocalPoint.ts   ← 純函式，RN 可用
├── buildCropRect.ts         ← 純函式，RN 可用
└── index.ts                 ← barrel
```

RN 版替換為 `react-native-image-crop-picker` 時，`shared/logic/` 零改動。

### 9. 多租戶資料夾結構（強制）

R2 bucket 的 key 命名**必須**包含 `tenantId`，確保不同租戶的資料隔離：

```
saome/
└── {tenantId}/
    └── {cardId}/
        ├── issuerLogo.png
        ├── backgroundImage.png
        └── icon.png
```

```typescript
// ✅ 正確：包含 tenantId
const key = `${env.R2_BUCKET}/${tenantId}/${cardId}/${imageType}.png`;

// ❌ 錯誤：缺少 tenantId，所有租戶混在同一層
const key = `${env.R2_BUCKET}/${cardId}/${imageType}.png`;
```

若 `tenantId` 來自 JWT middleware（`c.get('tenantId')`），route handler 從 `c` 注入；若是 public endpoint（不需登入），前端 POST 時帶上 `tenantId`，後端 validate 後寫入 key。

### 10. 同檔名覆蓋舊檔（強制）

每個 imageType 的檔案名稱**固定**，不做 UUID versioning，每次上傳覆蓋同一個 key：

```
# ✅ 正確：固定檔名，每次上傳覆蓋舊檔
saome/{tenantId}/{cardId}/issuerLogo.png      ← 上傳 → 覆蓋
saome/{tenantId}/{cardId}/backgroundImage.png  ← 上傳 → 覆蓋

# ❌ 錯誤：UUID 版本化 → 舊檔殘留，R2 空間浪費，且 DB key 容易 drift
saome/{tenantId}/{cardId}/issuerLogo-{uuid-v4}.png
```

```typescript
// ✅ 正確：固定的檔名（不含 UUID）
const key = `${env.R2_BUCKET}/${tenantId}/${cardId}/${imageType}.png`;

// ❌ 錯誤：用 Date.now() 或 UUID 版本化
const key = `${env.R2_BUCKET}/${tenantId}/${cardId}/${imageType}-${Date.now()}.png`;
```

**理由**：
- R2 定價按儲存用量收費，UUID 版本化累積舊檔造成浪費
- 每個 `imageType` 在 DB `settings` 內只有一個 key，覆蓋就是更新同一個 key
- 不需要版本歷史（卡片編輯器可以 undo，不需要 restore 舊圖）

**唯一的例外**：若業務需要版本歷史（如「還原到上一版」），才考慮 UUID 版本化，但那時應該存在另一個 `versions` subfolder，而非直接覆蓋當前 key。

### 11. Crop Window Invariant — Mask Size 必須對齊 Export Crop Size（強制）

> **觸發**：任何有 zoom (scale) 的圖片裁切 UI。LogoUploader / BackgroundUploader / IconUploader 都適用。

#### 設計意圖

Crop window（UI 上白色邊框 + 暗色遮罩挖洞）是**使用者選定的範圍指示器**，不該隨 zoom 變化。zoom 的語意是「在選定範圍內看到更多 src 細節」，不是「選更大範圍」。

#### 三層結構（強制）

```
outer container (fixed layout, pointer events)
├── inner canvas (transform: scale(scale), 只含 image)
├── SVG mask (NOT scaled, fixed cropWindowSize × cropWindowSize hole at center)
└── border (NOT scaled, fixed cropWindowSize × cropWindowSize frame at center)
```

| 層 | 是否套 scale | 角色 |
|----|--------------|------|
| outer container | ❌ | Layout 邊界，避免下方 slider 跳動 |
| inner canvas + image | ✅ | Zoom in 看到 src 細節 |
| SVG mask + border | ❌ | Crop 框永遠固定 |

#### 禁止（硬規則）

- ❌ 將 image + mask + border 全部包在同一個 `transform: scale(scale)` 內
- ❌ `cropImage()` 用 `min(NW, NH) / scale` 算 srcSquareSize（會跟 UI mask size 不一致）
- ❌ 把 `cropWindowSize` 跟 `baseCanvasWidth` 硬編碼在 hook 內（必須從 component 傳入）

#### srcSquareSize 公式（cropImage 必須遵守）

UI mask 在 src 中的對應 square size：

```
srcSquareSize = (cropWindowSize / (baseCanvasWidth * scale)) * naturalWidth
```

對齊 src 中央的條件（必須滿足）：

```
baseCanvasWidth / naturalWidth = baseCanvasHeight / naturalHeight
```

實作上 `baseContainerH = baseContainerW * NH / NW` 已保證 canvas aspect = src aspect（image fit 100% 無 letterbox）。

#### 範例（src 1024×768, base 400×300, mask 200×200）

| scale | image visual size | mask 200×200 占 image 比例 | srcSquareSize |
|-------|-------------------|-----------------------------|---------------|
| 1.0   | 400×300           | 50% × 67%                   | 512×512       |
| 2.0   | 800×600           | 25% × 33% (4x 細節)         | 256×256       |
| 3.0   | 1200×900          | 17% × 22% (9x 細節)         | ~171×171      |

#### Hook 簽名（強制）

```typescript
useImageCrop({
  outputWidth, outputHeight,
  cropWindowSize,    // 必填：UI mask size in CSS px (e.g. 200)
  baseCanvasWidth,   // 必填：UI canvas width in CSS px (e.g. 400)
  minScale, maxScale, initialScale,
});
```

#### 每個 imageType 對應的 crop window 設定

| imageType        | cropWindowSize | baseCanvasWidth | 用途 |
|------------------|----------------|------------------|------|
| `issuerLogo`     | 200            | 400              | LogoUploader |
| `backgroundImage`| 800            | 800              | BackgroundUploader |
| `icon`           | 256            | 256              | IconUploader |

詳見 `.cursor/skills/saome-image-upload/SKILL.md` § Crop Window Invariant 與 `apps/frontend/src/hooks/useImageCrop.ts` 實作。

#### 為什麼這是鐵律

1. **語意正確**：scale 改變 = zoom in 看細節，不是「選更大範圍」
2. **Layout 不變**：outer container 不被 scale 撐大，下方 slider 不會跳動
3. **Export 對齊 UI**：`srcSquareSize` 公式嚴格保證 export region = UI mask 在 src 中的對應 region
4. **RN migration 友善**：公式只跟數字相關，無 DOM 依賴

事故紀錄：`runs/improvements/feedback/20260826-0827-logo-crop-zoom-full-trace.md`。
決策：`runs/decisions/2026-08-27-logo-crop-zoom-invariant-mask.md`。

### 12. Stage Height Invariant — Stage 必須包含 Mask + Padding（強制）

> **觸發**：任何 image crop 元件（LogoUploader / BackgroundUploader /
> IconUploader 都適用）。**不是**只有 mobile UX 才有的 invariant——
> 是 crop 元件的**結構性**鐵律，desktop / tablet / mobile 全部適用。

#### 問題

舊設計假設：

```
stage = aspect-matched image 高度
outer = max(stage, maskH + 2 * PADDING)  ← outer 自己加 padding
frame = outer 的 absolute sibling，居中於 outer
```

對 landscape 圖片（aspect-matched height < mask height）：
- stage 是 360×120，outer 是 360×224
- frame 200×200 居中於 outer，跨越 bright image 與 dark padding
- 使用者看到「frame 超出 stage / 容器」

事故紀錄：`runs/improvements/feedback/20260830-logo-uploader-landscape-frame-exceeds-container.md`。
前四次 fix（stale closure / drag stutter / chain min-w-0 / iPhone 12 Pro Max double padding）都是症狀層
修補，保留了「stage = aspect, outer padded」這個錯誤的結構假設，只在 padding 數字 try-and-error。
第五次（round 4, 2026-08-30）才改變結構假設本身。

#### 解法（強制）

```typescript
// FRAME_PADDING 走 design token（Tailwind md = 16px）
const FRAME_PADDING = 16;
const aspectMatchedH = baseContainerW * (naturalHeight / naturalWidth);
const baseContainerH = Math.max(
  aspectMatchedH,
  responsiveCropWindow + 2 * FRAME_PADDING,
);
// outer = stage（outer 不再獨立 padding）
const outerContainerH = baseContainerH;
```

#### 各 aspect 行為

| Aspect | stage height | 視覺效果 |
|---|---|---|
| Portrait / Square | aspect-matched（自然就 ≥ mask+padding）| 沒變化 |
| Landscape | max(aspect, mask+padding) | stage extends vertically，image 在 stage 內 letterbox（object-fit: contain），frame 完全在 stage 內 |

對 landscape 來說，stage 高於 image，image 在 stage 內上下 letterbox；dark stage 背景填補
letterbox 區域，與 outer / stage 同一 dark bg 視覺一致。

#### 為什麼不直接縮小 frame 適應 stage（禁止替代方案）

| Scheme | Landscape 視覺 frame | 問題 |
|---|---|---|
| Stage extends（✅ 採用）| 200×200 | frame = export size，使用者心智一致 |
| Frame shrinks（❌ 禁止）| min(w*0.6, h*0.6, 200) | landscape 變成 72×72，與 200×200 export 不一致，使用者困惑 |

#### 禁止

- ❌ `outerContainerH = max(baseContainerH, maskH + 2 * PADDING)` 而 `baseContainerH` 只有 aspect-matched（frame 會超出 stage）
- ❌ Frame 是 outer 的 absolute sibling 並居中於 outer（應居中於 stage，或 outer === stage）
- ❌ 用「縮小 frame 適應 stage」替代（frame 200×200 是 export contract，不能縮）
- ❌ FRAME_PADDING 用 hardcoded 數字（如 8, 12, 20）而不走 design token

#### 為什麼這是鐵律

1. **語意正確**：stage = 「我要 crop 的區域 + frame 視覺」，outer = stage 本身，不該有額外 padding
2. **User mental model 對齊**：frame 永遠在 stage 內 = 在選定範圍內，不會「frame 跑出去」
3. **Landscape 不再有 surprise**：所有 aspect ratio 行為統一，沒「landscape 特例」
4. **SVG mask 正確顯示**：stageH ≥ maskH，SVG mask y = `(stageH - maskH) / 2` 永遠 ≥ 0，dark overlay 在 frame 周圍正確顯示
5. **跟 native photo cropper 行為一致**：iOS / Android photo cropper 對 landscape 都是 frame 固定 + image letterbox

#### 必跑 invariant test

```typescript
it('invariant: stageH >= maskH + 2 * FRAME_PADDING for ALL aspect ratios', () => {
  const cases = [
    { name: 'portrait 1:3',          w: 1000, h: 3000, parentW: 376 },
    { name: 'square 1:1',            w: 1000, h: 1000, parentW: 376 },
    { name: 'mild landscape 16:9',   w: 1920, h: 1080, parentW: 376 },
    { name: 'landscape 3:1',         w: 3000, h: 1000, parentW: 376 },
    { name: 'extreme landscape 15:1', w: 3000, h:  200, parentW: 376 },
  ];
  for (const c of cases) {
    // render, assert: stageH >= maskH + 32 (2 * 16 FRAME_PADDING)
  }
});
```

詳見 `.cursor/skills/saome-image-upload/SKILL.md` § Stage Height Invariant 與
`apps/frontend/src/components/business/dashboard/CardBuilderEditor/LogoUploader/LogoUploader.tsx`
內的「Stage height invariant」JSDoc 區塊。

#### 與 § 11 的分工

| § | 不變量 | 對齊目標 |
|---|---|---|
| 11 | mask size === export srcSquareSize | UI ↔ export |
| **12** | **stage height ≥ mask + padding** | **stage ↔ mask** |

### 13. Image Cache Busting — Fixed Filename + Version Query Param（強制）

> **觸發**：任何 R2 image upload（LogoUploader / BackgroundUploader / IconUploader）。
> § 10 規定 R2 用固定檔名（每次上傳覆蓋同一 key），但這跟 HTTP cache 衝突。

#### 問題

§ 10 規定 R2 用固定檔名（`{tenantId}/{cardId}/issuerLogo.png`），每次上傳覆蓋同一 key。
但這跟 HTTP cache（`Cache-Control`）衝突：

- Backend `getImage` 端點設 `Cache-Control: public, max-age=31536000`（1 年）
- 上傳後 URL 沒變（仍是 `/api/cards/{id}/image/logo`）
- 瀏覽器 / Cloudflare edge / R2 三層 cache 全部 return 舊檔
- 使用者看到「上傳成功但圖沒變」

事故紀錄：`runs/improvements/feedback/20260830-logo-image-cache-busting.md`（待寫）

#### 解法（強制）

**三層必須同時存在**：

| 層 | 設定 | 為什麼 |
|---|---|---|
| Backend `Cache-Control` | `public, max-age=31536000` | 讓 cache 確實生效，1 年內不重複打 R2 |
| Backend 檔名 | 固定（§ 10）| R2 不浪費空間 |
| Frontend URL | 加 `?v=${version}` query param | 前端能強制 bust cache |

#### 實作 pattern

**Store**（zustand 或 context）：每個 imageType 一個 `xxxVersion: number`，set 時 bump：

```ts
// apps/frontend/src/components/business/dashboard/CardBuilderEditor/CardBuilderEditor.store.ts
issuerLogoVersion: 0,
setIssuerLogo: (issuerLogo) => set({ issuerLogo, issuerLogoVersion: Date.now() }),
```

**Component**（圖片預覽元件）：URL 內加 `&v=${xxxVersion}`：

```tsx
// PassCardPreviewHeader.tsx
const logoUrl = `${api.baseUrl}${api.paths.cardImage(templateId, 'logo')}?token=...&v=${issuerLogoVersion}`;
```

**Upload 完成 callback**：必須呼叫 `setIssuerLogo(key)`（或對應 setter）自動 bump version。

#### 禁止

- ❌ 改用 UUID 檔名（`{uuid}.png`）— 違反 § 10、浪費 R2 空間、DB key 易 drift
- ❌ 改 `Cache-Control: no-store` — 等於放棄 cache，每次都打 R2
- ❌ Upload 後忘記 bump version — 上傳後圖不變
- ❌ 版本用全域 `Date.now()`（多個元件 race condition）— 用 store-bumped version

#### 必跑 conformance test

```ts
it('image URL includes version query param', () => {
  const store = useCardBuilderStore.getState();
  const before = store.issuerLogoVersion;
  store.setIssuerLogo('test-key');
  expect(store.issuerLogoVersion).toBeGreaterThan(before);
});
```

#### 與其他章節的分工

| § | 不變量 |
|---|--------|
| § 10 | R2 用固定檔名 |
| § 11 | mask size === export srcSquareSize |
| § 12 | stage height ≥ mask + padding |
| **§ 13** | **URL 含 version query param** |

### 14. Image Auth Strategy — Query Token vs Cookie vs Signed URL（強制）

> **觸發**：設計 `getImage` 端點（`/api/cards/:id/image/:type`）的 auth 策略。
> 必須考慮 `<img src>` 跟 RN `<Image source>` 的限制。

#### 三方案比較

| Auth 方案 | `<img src>` (web) | `<Image source>` (RN) | Cache 友善 | 實作成本 |
|---|---|---|---|---|
| `Authorization: Bearer ...` header | ❌ 不送 header | ⚠️ 需手動設 header | N/A | 中 |
| **`?token=JWT` query param（✅ 採用）** | ✅ 自動 | ✅ 自動 | ✅ | **低** |
| Cookie (`HttpOnly + SameSite=Lax`) | ⚠️ CORS `credentials: 'include'` | ⚠️ RN 預設不送 | ✅ | 中 |
| R2 Presigned GET URL (TTL) | ✅ | ✅ | ✅ | 高（後端加簽署 endpoint） |

#### 決策：保留 query token

```ts
// apps/backend/src/shared/middleware/auth.ts
export const requireAuth: MiddlewareHandler<HonoEnv> = async (c, next) => {
  const authHeader = c.req.header('Authorization');
  let token: string | undefined;
  if (authHeader?.startsWith('Bearer ')) {
    token = authHeader.slice(7);
  } else {
    // Try ?token= query param (for <img> requests without cookies)
    token = c.req.query('token');
  }
  if (!token) throw new AuthError(...);
  // verify + continue
};
```

**理由**：

- `<img src>` 跟 RN `<Image>` 都**不送 Authorization header**（瀏覽器/RN 自動 fetch 才送）
- Query token 是唯一「兩邊都自動送」的方案
- Token 有效期 1 年（登入體驗），query string 在 URL bar 暴露的風險可控（token 跟其他 R2 key 等級的敏感性）
- 切 cookie 會犧牲 cacheability（cookie-based request 通常不走 edge cache）

#### URL pattern（強制）

```
GET /api/cards/{templateId}/image/{logo|background|icon}?token={JWT}&v={version}
                                            ↑ query token    ↑ § 13 cache bust
```

兩個 query param 順序固定：`token` 先、`v` 後。

#### 切換時機（記錄在 rule）

**以下任一情況才考慮切換**：

1. RN 上線且需要 native image cache（`<Image>` 預設 cache 行為不同）
2. Token 改成短 TTL（如 15 分鐘）→ query token 暴露風險變高
3. Multi-tenant isolation 變成 per-IP 限制（cookie 較難 IP 限制）

否則保持 query token。

#### 禁止

- ❌ 改用 cookie 但前端 `<img>` 沒設 `credentials: 'include'` → 圖片永遠 401
- ❌ 改用 presigned GET URL 但忘記加 `?v=` cache bust → 上傳後圖不變
- ❌ Token 放在 path（`/image/{token}/{type}`）→ 路徑會被 log 記錄，安全性較差

決策紀錄：`runs/decisions/2026-08-30-image-auth-strategy-query-token.md`（待寫）

## L2 Checklist 對齊

| 動作 | 標準 |
|---|---|
| i18n namespace | **先建立** 獨立的 component-bound namespace（如 `logoUpload`），**禁止**放在 feature namespace（如 `cardBuilder`） |
| 元件結構 | `components/business/dashboard/CardBuilderEditor/LogoUploader/` 一個資料夾 |
| Test | `LogoUploader.test.tsx` + RTL |
| Storybook | `LogoUploader.stories.tsx` |

詳見 Rule 023 § 元件化原則。

## 觸發關鍵字

「上傳圖片」「R2」「presigned URL」「CORS 403」「LogoUploader」「BackgroundUploader」「裁切」「crop」「zoom」「scale」「mask」**必須**引用此 rule。

「cache」「Cache-Control」「上傳後圖沒變」「image preview stale」「URL versioning」「`?v=` query」→ 必引 § 13 Image Cache Busting。

「image 401」「`<img>` auth」「RN `<Image>` auth」「query token」「presigned GET URL」→ 必引 § 14 Image Auth Strategy。

「mobile」「viewport」「拖曳」「drag stutter」「chain overflow」「min-w-0」「pointermove」「momentum」「VIEWPORT_PADDING」「useCallback stale closure」「flex chain overflow」→ 必引 `frontend/029-image-crop-mobile-ux.mdc`（Rule 029：mobile drag UX 三軸 + layout chain + stale closure）。

## 參照

- `runs/improvements/feedback/20260823-logo-uploader-full-trace.md`
 — LogoUploader 完整 bug trace + Decision Log
- `runs/improvements/feedback/20260826-0827-logo-crop-zoom-full-trace.md`
 — Logo crop zoom 完整 trace（mask invariant + srcSquareSize 公式）
- `runs/improvements/feedback/20260830-logo-uploader-portrait-stale-closure.md`
 — 直立圖片裁切位置偏移（useCallback stale closure Bug-φ）
- `runs/improvements/feedback/20260830-logo-uploader-mobile-drag-stutter.md`
 — 手機拖曳跳格（React reconciliation 拖慢 drag）
- `runs/improvements/feedback/20260830-logo-uploader-chain-min-w-zero.md`
 — crop stage 把整頁撐開（flex chain min-width: auto）
- `runs/improvements/feedback/20260830-logo-uploader-iphone12promax-double-padding.md`
 — iPhone 12 Pro Max 雙層 padding 撐開
- `runs/improvements/feedback/20260830-logo-uploader-landscape-frame-exceeds-container.md`
 — landscape frame 超出 stage（結構性 fix，改變「stage=aspect, outer padded」假設）→ § 12
- `runs/improvements/feedback/20260830-cardbuilder-header-overflow-phones.md`
 — CardBuilderHeader h1+Steps overflow，Outlet inner min-w-0 修法
- `runs/improvements/feedback/20260830-logo-image-cache-busting.md`
 — Image Cache Busting 三層同時存在 pattern → § 13
- `runs/decisions/2026-08-30-image-auth-strategy-query-token.md`
 — Image auth strategy 保留 query token 決策 → § 14
- `runs/decisions/2026-08-27-logo-crop-zoom-invariant-mask.md`
 — Crop Window Invariant 決策
- `.cursor/skills/saome-image-upload/SKILL.md` § Crop Window Invariant + § Stage Height Invariant + § Mobile Drag UX
- `.cursor/rules/frontend/024-mobile-future-proof.mdc` § Hook Split Pattern
 — `.web.ts` / `.native.ts` 雙檔結構（useImageCrop 範例）
- `.cursor/rules/frontend/029-image-crop-mobile-ux.mdc`
 — mobile drag UX 三軸 + layout chain + stale closure（Rule 029）
- `apps/backend/src/modules/cards/routes/generate-upload-url.ts`
 — presigned URL 實作範例
- `apps/backend/src/modules/cards/routes/getImage.ts`
 — Image endpoint with `Cache-Control: 1 year` + query token auth (§ 13 / § 14 實作)
- `apps/backend/src/shared/middleware/auth.ts`
 — Dual-mode auth（Authorization header + ?token= query）
- `apps/backend/r2-cors.json`
 — CORS 設定檔
- `apps/frontend/src/hooks/useImageCrop.ts`
 — `cropImage()` 實作 + `srcSquareSize` 公式
- `apps/frontend/src/hooks/useImageCrop.web.ts`
 — Web Canvas-based cropImage（§ 14 平台分流範例）
- `apps/frontend/src/hooks/useImageCrop.native.ts`
 — Native stub（throw NotImplementedError）
- Rule 027（postgres dynamic query）— Settings merge pattern
- Rule 023（shared package / i18n namespace）— component-bound namespace 原則
- Rule 024（mobile future-proof）— RN 化裁切 logic 抽離要求 + Hook Split Pattern
