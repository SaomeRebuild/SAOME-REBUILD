---
description: Cloudflare Worker fetch 出口有 3 條路徑，只有「Worker entry boundary 包裹」能覆蓋 runtime-emitted error — 任何 backend Worker 必套 3 層 CORS defense pattern
alwaysApply: true
---

# Worker Runtime CORS Defense（MANDATORY）

> 觸發：寫任何 Cloudflare Worker（Hono / Workers / Pages functions / 自寫 fetch handler）、改 backend Worker 的 default export、debug「瀏覽器看到 CORS drop 但 curl 看不到」、deploy backend Worker 前。

## 背景

2026-09-07 production 登入 CORS drop：Worker runtime 直接 emit 503（沒進 Hono chain），瀏覽器 silent drop，使用者看到「Network error」，curl 完全看不出來。完整 trace：`runs/improvements/feedback/20260907-cors-runtime-503-fix.md` + `DEV/09-2026/0907-cors-runtime-503-fix.md` + `runs/decisions/2026-09-07-three-layer-cors-defense.md`。

SAOME 自 2026-07-28 Bug-4c 起的 fingerprint：**curl 看起來通、瀏覽器不通**。任何 silent CORS drop 都會有這個對稱性。

## Cloudflare Worker fetch 的 3 條出口

```
                                  ┌─────────────────────────────┐
HTTP request  ──▶ Worker fetch ──┤ ① runtime-level error path  │  ← 503 由 Cloudflare 發出
                                  │    (binding failure, CPU/   │     **完全沒進 Hono**
                                  │     memory limit, etc.)     │     CORS coverage: 無
                                  ├─────────────────────────────┤
                                  │ ② Hono `app.onError()`     │  ← route handler throw
                                  │    (SaomeError / unknown)   │     CORS coverage: errorHandler 內
                                  ├─────────────────────────────┤     `applyCorsHeadersToResponse`
                                  │ ③ Hono `app.fetch()`        │  ← 正常 response
                                  │    (route handler 完成)     │     CORS coverage: corsMiddleware
                                  └─────────────────────────────┘     `wrap-after-next`
```

每一條出口對應不同的 CORS coverage。任何 backend Worker 都必須 3 條全部覆蓋，否則 runtime-emitted error 會 silent CORS drop。

## 鐵律

### 1. 三層 CORS Defense 必齊（全 Worker 適用）

| 層 | 位置 | 覆蓋 | 來源 |
|---|---|---|---|
| **Layer 1** | `shared/middleware/cors.ts::corsMiddleware`（Hono chain 內，wrap-after-next）| 路徑 ③（正常 response）| env-driven allow-list（`ALLOWED_ORIGINS` + `ALLOWED_ORIGIN_PATTERNS`）|
| **Layer 2** | `shared/middleware/errorHandler.ts::applyCorsHeadersToResponse`（Hono `onError` 內 inline 私有）| 路徑 ②（route handler throw）| 共用 Layer 1 的 `resolveAllowedOrigin` |
| **Layer 3** | `shared/middleware/runtimeCors.ts::ensureCorsOnResponse`（Worker entry boundary）| 路徑 ①（runtime-emitted error）| **硬編 allow-list**（不靠 env，因為 entry boundary 拿不到 `c.env`）|

### 2. Worker entry boundary 必包 `ExportedHandler.fetch`

任何 Cloudflare Worker 的 default export **不可直接 export Hono app**，必須包成 `ExportedHandler.fetch` 包裹：

```ts
// ✅ 正確：Worker entry boundary 包裹 + Layer 3 CORS injection
const worker: ExportedHandler<HonoEnv['Bindings']> = {
  async fetch(request, env, ctx) {
    try {
      const res = await app.fetch(request, env, ctx);
      return ensureCorsOnResponse(request, res);
    } catch (err) {
      console.error('[worker.fetch] uncaught error:', err);
      const fallback = new Response(
        JSON.stringify({ error: { code: 'INTERNAL_ERROR', message: ... } }),
        { status: 503, headers: { 'Content-Type': 'application/json' } },
      );
      return ensureCorsOnResponse(request, fallback);
    }
  },
};
export default worker;

// ❌ 錯誤：直接 export Hono app — runtime-emitted error 不會有 CORS header
export default app;
```

### 3. Layer 3 必用硬編 allow-list（不讀 env）

理由：Worker entry boundary 拿不到 `c.env`（request 還沒進 Hono）。三個選擇的取捨：

| 方案 | 結果 |
|---|---|
| 把 env 解析邏輯寫死一份 | 違反 DRY；wrangler 改 env 後 deploy 完 runtimeCors 不知道 |
| **硬編 host Set + pattern regex** ✅ | deploy 不依賴 env 載入；entry 仍能運作 |
| 從 `env` 讀（重複解析）| 增加 entry 複雜度，沒好處 |

選硬編 — 寧可犧牲 DRY 換「Hono 整個掛了，CORS 還在」。文件註解明寫「wrangler.jsonc 新增 production origin 要同步兩處」。

### 4. 同步責任 SOP（MANDATORY — 新增 production origin 必跑）

未來新增任何 production origin 到 backend Worker：

1. **必跑** `wrangler.jsonc::vars.ALLOWED_ORIGINS` 加新 origin（或 `ALLOWED_ORIGIN_PATTERNS` 加新 pattern）
2. **必跑** `apps/backend/src/shared/middleware/runtimeCors.ts::RUNTIME_ALLOWED_HOSTS` 加新 host（或 `RUNTIME_ALLOWED_HOST_PATTERNS` 加新 regex）
3. **同一個 PR** 帶齊上面兩個改動 + 一條 conformance test 驗證新 origin 被兩處都接受
4. **PR 描述必填**「同步加進 wrangler.jsonc 跟 runtimeCors.ts」checklist
5. **deploy 後必跑** smoke test：curl `OPTIONS /api/auth/login` 帶新 origin 必須回 CORS header；curl `POST /api/auth/login` 帶新 origin 即使 401 也必須回 CORS header（Rule 017 § backend Worker CORS post-deploy check）

### 5. Conformance test 必覆蓋（MANDATORY — Layer 3 不可缺）

`apps/backend/src/shared/middleware/runtimeCors.test.ts` 必含 6 條：

| # | Test | 覆蓋 |
|---|---|---|
| 1 | `isRuntimeAllowedHost` exact match | production workers.dev / saome.org |
| 2 | `isRuntimeAllowedHost` pattern match | Pages preview subdomain |
| 3 | `isRuntimeAllowedHost` 拒絕 unrelated host | 防 evil.example.com / localhost / apex |
| 4 | `isRuntimeAllowedHost` case-insensitive | host.toUpperCase() |
| 5 | `ensureCorsOnResponse` 帶 allow-list origin 的 503 → 加 CORS header | **本次事故回歸測試** |
| 6 | `ensureCorsOnResponse` 帶 evil origin → 不加 CORS header | 安全邊界 |
| 7 | `ensureCorsOnResponse` preserve existing headers | 與 requestId 等 middleware 並存 |
| 8 | `ensureCorsOnResponse` works on 200 | defense-in-depth（正常 path 也走）|

加上既有 `corsMiddleware.test.ts`（Bug-4d regression）+ `errorHandler.test.ts`（SaomeError CORS regression），3 個測試檔共同 pin 死 3 層防禦的行為。

### 6. 為什麼 curl 看不到這條 bug

```bash
curl https://saome-backend.josh1989213.workers.dev/api/auth/login
# → 503（runtime 拒絕）
# curl 不在 HTTPS origin 內，沒有 CORS 規則，503 直接 body 給你看
```

**curl 看起來是「backend 503」，跟「正常 503」無法區分**。只有從 HTTPS origin 內 fetch 才會觸覽器 CORS drop，這是 SAOME 自 2026-07-28 Bug-4c 起的 fingerprint。

### 7. 未來新 Worker 必套同樣 3 層 pattern

任何新增 Cloudflare Worker（`saome-api` / `saome-cron-worker` / `saome-image-worker` / `saome-webhook-worker` 等）必走同樣 SOP：

1. Layer 1：Hono `corsMiddleware`（或 framework equivalent）wrap-after-next
2. Layer 2：error handler 內 inline CORS injection
3. Layer 3：Worker entry boundary 包 `ExportedHandler.fetch`，跑 `ensureCorsOnResponse`
4. 3 個測試檔分別覆蓋 3 層
5. wrangler.jsonc 跟 runtimeCors.ts 同步更新

第二個 Worker 出現時，把 `isRuntimeAllowedHost` + `ensureCorsOnResponse` 抽到 `packages/shared/lib/runtimeCors.ts` 共用。

### 8. Cold Start Dead Zone — 後端救不了，前端必須硬化（2026-09-09 新增）

**3 層 CORS defense 的邊界**：Layer 3 是 **last-line-of-defense 但不是 impossible-line-of-defense**。在 **Worker isolate cold start 早期**（isolate 還沒載到 entry code 就 emit 的 503）情況下：

```
Hyperdrive binding 解析 timeout
   ↓
Worker runtime 直接 emit 503（在 entry code 載入之前）
   ↓
Layer 3 ensureCorsOnResponse 沒機會跑 ← 真正的 dead zone
   ↓
Browser fetch silent drop → TypeError("Failed to fetch")
```

**修法不在後端**（沒有 Cloudflare runtime hook 可以讓我們在「entry code 載入前」inject CORS），**必須前端硬化**。兩條並存：

#### 8.1 Layer 4 — LoginPage mount 背景 ping `/health`（預防）

[apps/frontend/src/pages/auth/LoginPage.tsx](apps/frontend/src/pages/auth/LoginPage.tsx) 加 useEffect：

```ts
useEffect(() => {
  const ctrl = new AbortController();
  const timer = window.setTimeout(() => {
    fetch(`${api.baseUrl}/health`, {
      method: 'GET',
      signal: ctrl.signal,
      credentials: 'omit',
    }).catch(() => {
      /* swallow — httpClient.network-retry handles real failures */
    });
  }, 100);
  return () => {
    ctrl.abort();
    window.clearTimeout(timer);
  };
}, []);
```

| 設計 | 原因 |
|---|---|
| `useEffect` mount-only + 100ms delay | 避開 React hydration 跟 initial paint 的競爭 |
| `AbortController` cleanup | unmount 時 abort stale fetch |
| `.catch(() => {})` fire-and-forget | warmup 失敗不該影響 user 看到 form |
| `credentials: 'omit'` | warmup 不需要 cookie |

只 warmup LoginPage（user 第一個 entry point）；不 warmup Dashboard / CardBuilder 等「登入後」page（cold start 只在第一個 request 發生）。

#### 8.2 Layer 5 — httpClient 加 network-error retry（兜底）

[apps/frontend/src/services/httpClient.ts](apps/frontend/src/services/httpClient.ts) 的 `requestWithRetry` 內包 try/catch fetchImpl：

```ts
const MAX_NETWORK_RETRIES = 2;
const NETWORK_RETRY_BASE_DELAY_MS = 500;

let res: Response;
try {
  res = await this.fetchImpl(url, { ... });
} catch (err) {
  if (err instanceof TypeError && attempt < MAX_NETWORK_RETRIES) {
    const delayMs = NETWORK_RETRY_BASE_DELAY_MS * Math.pow(2, attempt);
    await new Promise((resolve) => setTimeout(resolve, delayMs));
    return this.requestWithRetry<T>(method, path, init, attempt + 1);
  }
  throw err;  // AbortError 或 retry 上限 → 給 caller 處理
}
```

| 設計 | 原因 |
|---|---|
| 只 retry `TypeError` | TypeError 是 transient（cold start CORS drop、ERR_FAILED、connection reset）|
| 不 retry `AbortError` | AbortSignal.timeout 是 user/timeout-initiated，不該 retry |
| `MAX_NETWORK_RETRIES = 2`（比 5xx retry 少）| 避免 user 等太久；加上 5xx retry 上限會疊到 6 次 |
| 與 5xx retry **完全獨立** | cold start 先走 network-retry（TypeError），warm 後走 5xx-retry（5xx response）|

#### 8.3 兩條必須並存

| 場景 | 只有 warmup | 只有 retry | 兩條並存 |
|---|---|---|---|
| 常見 cold start | 修好 | user 多等 500-1000ms | user 無感 |
| Warmup race window（< 100ms）| leak | 修好 | retry 兜底 |
| Worker 完全 down | user 看到 error | user 看到 error | user 看到 error（沒 regression）|

#### 8.4 完整 5 層 defense-in-depth 演進

| 時期 | Defense | 場景 | 沒覆蓋 |
|---|---|---|---|
| 2026-07-28 Bug-4c | frontend Mixed Content | 前端 bundle | 後端 |
| 2026-07-28 Bug-4d | `ALLOWED_ORIGINS` 補 Workers preview subdomain | Hono path ②③④ | runtime ① |
| 2026-09-05 Phase 3.3 | `corsMiddleware` wrap-after-next + `errorHandler` 內 CORS | Hono path ②③④ | runtime ① |
| 2026-09-07 | `ensureCorsOnResponse` Worker entry boundary | runtime ① (warm 後) | cold start 期間 |
| **2026-09-09（這次）** | **LoginPage warmup + httpClient.network-retry** | **cold start TypeError 兜底** | — |

#### 8.5 Frontend regression test 必覆蓋

新增測試檔 / describe block 必含：

| 測試 | 覆蓋 |
|---|---|
| LoginPage mount 後 100ms fetch 呼叫 1 次 | 100ms delay 設計 |
| unmount 後 `signal.aborted === true` | AbortController cleanup |
| 已認證 user 走 Navigate redirect 時 warmup useEffect 仍跑 | mount 觸發順序 |
| httpClient TypeError 第一次 + 第二次 success → 2 次 fetch | 基本 retry |
| httpClient TypeError 連續 3 次 throw TypeError → 3 次 fetch | retry 上限 |
| httpClient AbortError throw immediately → 1 次 fetch | timeout 不 retry |
| httpClient TypeError → 503 → 503 → 200 → 4 次 fetch | network + 5xx retry 鏈 |

完整討論見：[runs/improvements/feedback/20260909-cold-start-dead-zone-network-error.md](../runs/improvements/feedback/20260909-cold-start-dead-zone-network-error.md)。

## 9. Root Cause Fix — Missing `scheduled` Handler（2026-09-09 新增）

> 2026-09-09 早上實測：0909 的 LoginPage warmup + httpClient retry 解決了「cold start 後 user 看到 Network error」的症狀，但**根本原因是**：`wrangler.jsonc::triggers.crons` 宣告了 `*/5 * * * *`（每 5 分鐘），但 `worker` 物件**從來沒有 `scheduled` 方法**。

### 為什麼 cron trigger 沒有暖化 Worker？

Cloudflare Cron Trigger 的行為：
1. `wrangler.jsonc::triggers.crons` 宣告排程
2. Cloudflare 在排程時間呼叫 Worker 的 `scheduled(event, env, ctx)` 方法
3. 如果 Worker **沒有** `scheduled` 方法 → cron 事件被 Cloudflare 默默丢掉，**沒有任何錯誤**
4. Worker isolate 照樣被 idle-eviction 踢掉 → 幾小時後第一個 user request 冷啟

關鍵：`warmupCron` 是 HTTP route（`GET /api/cron/warmup`），不是 `scheduled` handler。兩者完全不同：
- **HTTP route**：需要外部 request 觸發
- **`scheduled` handler**：Cloudflare cron trigger 在內部呼叫，**不需要外部 request**

### 為什麼 wrangler deploy output 沒有說 cron handler 找不到？

Cloudflare 的設計：**如果 Worker 沒有 `scheduled` handler，cron 就默默失敗，不噴 error**。這讓問題難以發現。直到這次，加上 `wrangler tail` 看即時 log 才發現。

### 修法：加 `worker.scheduled`

`src/index.ts` 的 `worker` 物件加 `scheduled` 方法（Phase 2026-09-09）：

```typescript
const worker: ExportedHandler<HonoEnv['Bindings']> = {
  async fetch(request, env, ctx) { ... },   // 既有的 Layer 3 CORS wrapper

  async scheduled(event, env, ctx) {
    // Purpose 1: HTTP layer keep-alive
    //   fetch(SaOME_BACKEND_URL/health) 暖化整個 Hono pipeline
    // Purpose 2: Hyperdrive keep-alive
    //   SELECT 1 防止 Hyperdrive >60s idle 斷線
    // Purpose 3: Billing cycle advancement
    //   原本 billingCycleCronRoute 是 HTTP route（沒被呼叫），現在正確鉤上
  },
};
```

所有錯誤用 `try/catch` 包起來：`console.warn`（wrangler tail 看得見）但不 throw。cron 的任何一步失敗不該 crash 整個 handler。

### wrangler.jsonc 同步更新

`wrangler.jsonc::triggers.crons` 從 `*/5 * * * *`（每 5 分鐘）改為 `*/2 * * * *`（每 2 分鐘）。`*/2` 更保守，確保 Worker isolate 不會被 idle-eviction 踢掉。

### deploy output 確認

有 `schedule` 行表示 `scheduled` handler 已正確綁定：

```
Deployed saome-backend triggers (2.06 sec)
  https://saome-backend.josh1989213.workers.dev
  schedule: */2 * * * *
```

之前沒有這行（因為沒有 handler）。

### 與 §8 的關係

| | §8 LoginPage warmup + httpClient retry | §9 scheduled handler |
|---|---|---|
| 位置 | 前端 | 後端 |
| 目的 | cold start 後的 UX 兜底 | **根本原因：防止 cold start** |
| 何時觸發 | user 訪問 `/login` 時 | 每 2 分鐘（cron）|
| 覆蓋 | 只覆蓋登入 flow | 覆蓋所有 API endpoint |

§8 + §9 必須並存：§9 預防、§8 兜底。

### 驗證

```bash
# 1. deploy output 有 schedule 行
$ wrangler deploy | grep "schedule"
  schedule: */2 * * * *

# 2. wrangler tail 看 cron 實際有跑
$ wrangler tail --format pretty
# 等 2 分鐘，應該看到：
# [scheduled] cron=*/2 * * * * http-warmup status=200
# [scheduled] cron=*/2 * * * * hyperdrive-keepalive ok=1
```

完整 trace：[runs/improvements/feedback/20260909-cold-start-scheduled-handler-missing.md](../runs/improvements/feedback/20260909-cold-start-scheduled-handler-missing.md)。

## 觸發關鍵字

「Cloudflare Worker」、「Hono middleware」、「CORS drop」、「Worker runtime」、「ExportedHandler」、「entry boundary」、「fetch handler」、「worker.fetch」、「503 + no CORS」、「瀏覽器 silent drop」、「curl 看不出來」、「`Access-Control-Allow-Origin`」**必須**引用本 rule。

「新增 backend Worker」、「新增 saome-api / saome-cron」、「wrangler.jsonc 新增 origin」、「同步兩處 allow-list」→ 必引 § 4 同步責任 SOP。

## 與其他 rule 關係

- `017-production-bundle-guard.mdc` § 為什麼 curl 不夠 + § backend Worker CORS post-deploy check — 對應
- `000-modular-design.mdc` Part B § B.4 禁止清單 — 加 `❌ Worker default export 直接 expose Hono app`
- `015-cloudflare-pages-deploy.mdc` § Workers runtime — 對齊
- `apps/backend/AGENTS.md` § 3 層 CORS defense — reference implementation

## 參照

- `runs/improvements/feedback/20260907-cors-runtime-503-fix.md` — 原事故 trace + 6 條 test
- `runs/improvements/feedback/20260909-cold-start-dead-zone-network-error.md` — cold start dead zone trace + 8 條 regression test（network-retry × 5 + warmup × 3）
- `DEV/09-2026/0907-cors-runtime-503-fix.md` — DEV LOG 時間軸 + 4 輪 defense-in-depth 演進
- `runs/decisions/2026-09-07-three-layer-cors-defense.md` — 三段式決策紀錄
- `apps/backend/src/shared/middleware/runtimeCors.ts` — Layer 3 實作
- `apps/backend/src/shared/middleware/runtimeCors.test.ts` — 6 條 conformance test
- `apps/backend/src/shared/middleware/cors.ts` — Layer 1（Hono chain wrap-after-next）
- `apps/backend/src/shared/middleware/errorHandler.ts` — Layer 2（Hono `onError` inline CORS）
- `apps/backend/src/index.ts` line 76-188 — Worker entry boundary + scheduled handler
- `apps/backend/src/index.scheduled.test.ts` — 8 條 scheduled handler test
- `apps/backend/wrangler.jsonc` — Layer 1+2 的 env-driven allow-list + triggers.crons
- `apps/frontend/src/services/httpClient.ts` — Layer 5 network-error retry 實作
- `apps/frontend/src/services/httpClient.test.ts` — 5 條 network-retry test
- `apps/frontend/src/pages/auth/LoginPage.tsx` — Layer 4 mount-time warmup
- `apps/frontend/src/pages/auth/LoginPage.test.tsx` — 3 條 warmup test
- commit `7935de8` — fix(backend): Worker-level CORS injection for runtime-emitted 503s
